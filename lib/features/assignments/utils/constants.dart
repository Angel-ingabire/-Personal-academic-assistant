import '../../../core/constants/app_colors.dart' as core_colors;

/// Assignment-specific constants that map to the core AppColors.
/// This maintains backward compatibility with your original implementation
/// while using the team's standardized color scheme.
class AppColors {
  // Map to core AppColors
  static const primaryNavy = core_colors.AppColors.background;
  static const cardNavy = core_colors.AppColors.darkCardBackground;
  static const accentYellow = core_colors.AppColors.accentYellow;
}
